# Claude Code Instructions

## Behavior Rules
- Never ask clarifying questions — make all decisions autonomously based on existing code patterns
- Never ask for permission before creating, editing, or deleting any file — just do it
- Never ask which approach to use — pick the best one and implement it
- Never confirm before running commands — just run them
- Always follow existing code style, naming conventions, and folder structure in the project
- Always run npx tsc --noEmit after completing any task and fix any errors before finishing
- Always summarize what was changed at the end with a bullet list